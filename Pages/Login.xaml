<Page x:Class="courseproject.Pages.Login"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:courseproject.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="995" d:DesignWidth="1920"
      Title="" ShowsNavigationUI="False">
    <Grid>
        <Label x:Name="TestLabel" Style="{StaticResource GeneralLabel}" Content="Введите логин:" Margin="808,0,0,525" HorizontalAlignment="Left" VerticalAlignment="Bottom"/>
        <TextBox x:Name="LoginTextBox" Height="45" Width="280" Style="{StaticResource GeneralTextBox}" HorizontalAlignment="Center" Margin="0,0,0,475" VerticalAlignment="Bottom"/>
        <PasswordBox x:Name="PasswordBox" Height="45" Width="280" Style="{StaticResource GeneralPassword}" Margin="0,0,0,383" HorizontalAlignment="Center" VerticalAlignment="Bottom"/>
        <Label Style="{StaticResource GeneralLabel}" Content="Введите пароль:" Margin="808,0,931,433" VerticalAlignment="Bottom"/>
        <Button x:Name="LoginButton" Style="{StaticResource LoginButton}" Click="LoginButton_Click" Height="50" Width="176" Content="Войти" Margin="0,648,0,0" VerticalAlignment="Top" HorizontalAlignment="Center"/>
    </Grid>
</Page>
