<Page x:Class="courseproject.Pages.Login"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:courseproject.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="995" d:DesignWidth="1620"
      Title="" ShowsNavigationUI="False">
    <Grid>
        <StackPanel Orientation="Vertical" VerticalAlignment="Center" HorizontalAlignment="Center" Margin="0, 0, 300 ,0">
            <Label Style="{StaticResource GeneralLabel}" Content="Введите логин:"/>
            <TextBox x:Name="LoginTextBox" Height="45" Width="280" Style="{StaticResource GeneralTextBox}"/>
            <Label Style="{StaticResource GeneralLabel}" Content="Введите пароль:"/>
            <PasswordBox x:Name="PasswordBox" Height="45" Width="280" Style="{StaticResource GeneralPassword}"/>
            <Button x:Name="LoginButton" Style="{StaticResource LoginButton}"
                    Click="LoginButton_Click" Height="50" Width="176" Content="Войти" Margin="0, 50"/>

        </StackPanel>
    </Grid>
</Page>
